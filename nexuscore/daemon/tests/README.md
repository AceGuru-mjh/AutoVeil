# Daemon 单元测试目录
#
# 当前为占位，未来添加：
# - test_codec: IPC 帧编解码
# - test_module_loader: manifest 解析
# - test_script_executor: shim 生成
# - test_util: copyFile / hashPath / etc
#
# 使用 GoogleTest，CMakeLists.txt 在父级 cmake 配置中通过 add_subdirectory(tests) 启用。
